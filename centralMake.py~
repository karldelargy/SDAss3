def centralMake(centralLine):
    max = centralLine['activeSize']
    count = 0
    while count < max:
        card = centralLine['deck'].pop()
        centralLine['active'].append(card)
        count = count + 1
    return(centralLine)
